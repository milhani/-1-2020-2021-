#include <iostream>

int main() {
    size_t n;
    std::cin >> n;
    float sum = 0.0, sgn = 1.0;
    for (size_t i = 1; i <= n; ++i) {
        sum += sgn / i;
        sgn *= -1.0;
    }
    std::cout << sum << std::endl;
    return 0;
}

